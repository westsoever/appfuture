⏺ Data download blocked until 16:00 when credentials go live. Smoke test
   will fail on data checks too, so holding that until after download.

  Status:
  - Download: blocked — credentials at 16:00
  - Smoke test: will run right after download succeeds

  At 16:00, do this in order:
  1. Claim API credits → get ANTHROPIC_API_KEY
  2. Update bucket_access/config.py with real credentials
  3. cd ~/aiw/vibraint && python download_data.py (large NIfTI files —
  start immediately)
  4. python smoke_test.py — verify columns and top region
  5. streamlit run app.py

  Want me to wait and remind you, or will you ping me when credentials
  are ready?